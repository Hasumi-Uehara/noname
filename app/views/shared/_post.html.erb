<li>
  <p class="timeline-date"><%= post.event_date %></p>
  <div class="timeline-content">
    <h2><%= post.event_name %> <i class="fas fa-stopwatch"></i><%= post.tournament_record %></h2>
    <%= link_to image_tag(post.image, class:"card__image"), "#" %>
    <p><%= post.text %></p>
  </div>
</li>
